# CapCut Sharing Platform - Production Environment Configuration
# ⚠️ CRITICAL: Never commit this file with real values to git!

# ==================================================
# SECURITY SETTINGS
# ==================================================

# SECRET_KEY: Used for JWT tokens and session encryption
# MUST be at least 32 characters
# Generate with: python -c 'import secrets; print(secrets.token_urlsafe(64))'
SECRET_KEY=YOUR_64_CHARACTER_RANDOM_SECRET_KEY_HERE

# Admin credentials for /admin dashboard
ADMIN_EMAIL=admin@yourdomain.com
# ADMIN_PASSWORD: Must be at least 12 characters, no weak passwords allowed
ADMIN_PASSWORD=YOUR_STRONG_ADMIN_PASSWORD_HERE

# Environment (development|staging|production)
ENVIRONMENT=production

# ==================================================
# DATABASE
# ==================================================

# PostgreSQL connection string
# Format: postgresql://username:password@host:port/database
DATABASE_URL=postgresql://prod_user:STRONG_DB_PASSWORD@your-db-host.com:5432/capcut_prod

# ==================================================
# FRONTEND & SERVICES
# ==================================================

# Your production frontend URL (must use HTTPS)
FRONTEND_URL=https://yourdomain.com

# Bot service URL (internal or external)
BOT_SERVICE_URL=http://bot-service:5000

# ==================================================
# PAYMENT - PAYSTACK
# ==================================================

# ⚠️ Use LIVE keys in production (starts with sk_live_)
# Get from: https://dashboard.paystack.com/#/settings/developer
PAYSTACK_SECRET_KEY=sk_live_YOUR_PAYSTACK_SECRET_KEY
PAYSTACK_PUBLIC_KEY=pk_live_YOUR_PAYSTACK_PUBLIC_KEY

# ==================================================
# EMAIL - GMAIL
# ==================================================

# Gmail account for sending emails and bot automation
GMAIL_USER=your-app-email@gmail.com

# Gmail App Password (NOT your regular Gmail password)
# Generate at: https://myaccount.google.com/apppasswords
# Format: xxxx-xxxx-xxxx-xxxx (16 characters)
GMAIL_APP_PASSWORD=xxxx-xxxx-xxxx-xxxx

# ==================================================
# OPTIONAL SETTINGS
# ==================================================

# IMAP settings for email reading (usually don't need to change)
IMAP_HOST=imap.gmail.com
IMAP_PORT=993

# Bot configuration
HEADLESS=true
PORT=5000

# ==================================================
# SECURITY CHECKLIST
# ==================================================
# Before deploying to production, ensure:
# ✅ SECRET_KEY is at least 32 characters (recommended 64)
# ✅ ADMIN_PASSWORD is strong (min 12 chars, not 'admin123')
# ✅ DATABASE_URL points to production database
# ✅ FRONTEND_URL uses HTTPS
# ✅ PAYSTACK_SECRET_KEY uses live key (sk_live_)
# ✅ All passwords are unique and strong
# ✅ This file is added to .gitignore
# ✅ Environment variables are set in hosting platform

# ==================================================
# HOW TO GENERATE STRONG SECRETS
# ==================================================

# Generate SECRET_KEY:
# python -c 'import secrets; print(secrets.token_urlsafe(64))'

# Generate ADMIN_PASSWORD:
# python -c 'import secrets; import string; chars = string.ascii_letters + string.digits + string.punctuation; print("".join(secrets.choice(chars) for _ in range(20)))'

# Generate DATABASE PASSWORD:
# python -c 'import secrets; import string; chars = string.ascii_letters + string.digits; print("".join(secrets.choice(chars) for _ in range(32)))'